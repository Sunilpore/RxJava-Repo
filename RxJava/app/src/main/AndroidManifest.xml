<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rxjavaeg1">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".operator.interval_and_timer.IntervalActivity">
        </activity>

        <activity android:name=".MainActivity">
        </activity>

        <activity android:name=".operator.interval_and_timer.TimerActivity" >
        </activity>

        <activity android:name=".operator.just_range_repeat.JustOperatorActivity" >
        </activity>

        <activity android:name=".operator.from_operator.FromArrayOperatorActivity" >
        </activity>

        <activity android:name=".operator.from_operator.FromIterableOperatorActivity" >
        </activity>

        <activity android:name=".operator.future.FutureActivity" >
        </activity>

        <activity android:name=".operator.publisher.PublisherActivity" >
        </activity>

        <activity android:name=".operator.filter.FilterActivity" >
        </activity>

        <activity android:name=".operator.distinct.DistinctFunActivity" >
        </activity>

        <activity android:name=".operator.take_and_takewhile.TakeActivity" >
        </activity>

        <activity android:name=".operator.take_and_takewhile.TakeWhileActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>